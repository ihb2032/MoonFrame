///|
/// Bring the test package's version of `assert_eq` into scope for every
/// blackbox test file in this package. The prelude version is deprecated in
/// favor of this Debug-based assertion.
using @test {assert_eq}
