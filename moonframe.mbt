// MoonFrame — facade package.
//
// This file re-exports the public API of the library so that users can
// `import "ihb2032/MoonFrame"` and access everything in one place.
//
// Re-exports are added in P13 once the underlying layers (types, column,
// frame, ops, io) are in place. For now this file is intentionally empty.
